{
  "name": "ascout",
  "version": "0.0.0",
  "private": true,
  "description": "Verify everything AI ships.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "ascout": "./dist/cli.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "cross-spawn": "7.0.6"
  }
}
